Algoritmo "parasomador"

Var

   n, c, s: inteiro

Inicio
   s <- 0
   para c <- 1 ate 5 faca
      escreval("DIgite um valor: ")
      leia(n)
      s <- s + n
      c <- c + 1
   fimpara
   escreval("A soma do valores é ", s)

Fimalgoritmo